{
  "name": "@ethernaut-solve/fallback",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ethernaut-solve/utils": "workspace:^1.0.0"
  }
}
