{
  "name": "@ethernaut-solve/elevator",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "rm -rf dist",
    "deploy:hardhat": "hardhat deploy",
    "deploy:rinkeby": "hardhat deploy --network rinkeby",
    "trigger": "hardhat --network rinkeby run scripts/trigger.ts"
  }
}
